def solution(rank, attendance):
    answer = [(rank[i], i) for i in range(len(rank)) if attendance[i]]
    answer.sort()
    a,b,c = answer[0][1],answer[1][1],answer[2][1]
    
    return 10000*a+100*b+c
