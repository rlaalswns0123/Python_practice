def solution(a, b):
    num_multi = str(a)+str(b)
    num_submulti = 2*a*b
    if int(num_multi) > num_submulti :
        answer = int(num_multi)
    else :
        answer = num_submulti
    return answer
